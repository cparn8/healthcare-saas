[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = test_*.py
markers =
    auth: authentication-related tests
    permissions: authorization and role-based access tests
    business_rules: domain and data integrity rules
    api: API endpoint tests
addopts = -q
env =
    PYTEST_RUNNING=1